import 'package:flutter/material.dart';

class PriceFormComponent extends StatelessWidget {
  const PriceFormComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
