import { SermonFilters } from './types';

export const DEFAULT_SERMON_FILTERS: SermonFilters = {
    ministry: 'all',
    title: '',
    speaker: '',
    dateFrom: '',
    dateTo: '',
    favoritesOnly: false,
};
