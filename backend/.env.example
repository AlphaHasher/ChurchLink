# Example environment variables for YouTube notifications
YOUTUBE_API_KEY=your_youtube_api_key_here
PRIMARY_CHANNEL_ID=your_channel_id_here

# Timezone for notifications and other local time operations
LOCAL_TIMEZONE=America/Los_Angeles

MONGODB_URL=localhost:27017

BACKEND_URL=http://localhost:8000
FRONTEND_URL=http://localhost:3000

ADDITIONAL_ORIGINS=http://localhost:3000,website.com

# This is from frontend is the apiKey in firebase config
FIREBASE_WEB_API_KEY=

# PayPal (sandbox/production)
PAYPAL_CLIENT_ID=
PAYPAL_CLIENT_SECRET=
PAYPAL_MODE=sandbox # Change to 'live' for production


# ======================================================
# ======================================================
# ======================================================
# Email Configuration Template for RefundEmailHelper
# Add these variables to your .env file to enable email notifications

# === Basic Email Configuration ===
# Church/Organization Information
CHURCH_NAME="Your Church Name"
CHURCH_WEBSITE="https://yourchurch.com"
ADMIN_EMAIL="admin@yourchurch.com"

# From Email Settings
FROM_EMAIL="noreply@yourchurch.com"
FROM_NAME="Your Church Name"

# Admin emails (comma-separated list for refund notifications)
ADMIN_EMAILS="admin1@yourchurch.com,admin2@yourchurch.com,finance@yourchurch.com"

# === Option 1: SMTP Configuration (Gmail, Outlook, etc.) ===
SMTP_SERVER="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USERNAME="your-email@gmail.com"
SMTP_PASSWORD="your-app-password"  # Use app password for Gmail

# Gmail App Password Setup:
# 1. Enable 2-factor authentication on your Google account
# 2. Go to Google Account settings > Security > App passwords
# 3. Generate an app password for "Mail"
# 4. Use the generated password here

# === Option 2: SendGrid Configuration (Alternative) ===
# SENDGRID_API_KEY="SG.your-sendgrid-api-key-here"

# === Option 3: Other SMTP Providers ===
# For Outlook/Hotmail:
# SMTP_SERVER="smtp-mail.outlook.com"
# SMTP_PORT="587"

# For Yahoo:
# SMTP_SERVER="smtp.mail.yahoo.com"
# SMTP_PORT="587"

# For Custom SMTP:
# SMTP_SERVER="mail.yourdomin.com"
# SMTP_PORT="587" or "465" for SSL