# =============================================================================
# ChurchLink Backend Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values
#
# DOCKER USERS: When running with docker-compose, the MONGODB_URL is
# automatically overridden to "mongodb:27017" for Docker networking.
# Keep MONGODB_URL=localhost:27017 in your .env file for local development.
# =============================================================================

# Database Configuration
# For local development: localhost:27017
# For Docker: This is automatically overridden by docker-compose.yml
MONGODB_URL=localhost:27017

# Backend Server Configuration
BACKEND_URL=http://localhost:8000

# Timezone for notifications and other local time operations
LOCAL_TIMEZONE=America/Los_Angeles

# YouTube API (optional - for video notifications)
YOUTUBE_API_KEY=your_youtube_api_key_here
PRIMARY_CHANNEL_ID=your_channel_id_here

# CORS Configuration
# NOTE: Frontend is served by the backend on the same origin in production
# ADDITIONAL_ORIGINS is only needed for development when running frontend separately with npm run dev
# Example for dev server: ADDITIONAL_ORIGINS=http://localhost:5173
# For production with multiple domains: ADDITIONAL_ORIGINS=https://www.yourchurch.com,https://yourchurch.com
ADDITIONAL_ORIGINS=

# This is from frontend is the apiKey in firebase config
FIREBASE_WEB_API_KEY=

# PayPal (sandbox/production)
PAYPAL_CLIENT_ID=
PAYPAL_CLIENT_SECRET=
PAYPAL_WEBHOOK_ID=
PAYPAL_MODE=sandbox # Change to 'live' for production

# Controls security validations for E2E_TEST_MODE, use development or production
ENVIRONMENT=development

# E2E Test Mode - ONLY for Cypress automated testing when set to true
# This Bypasses ALL authentication - NEVER enable in production!
E2E_TEST_MODE=false

